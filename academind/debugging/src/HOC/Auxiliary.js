import React from "react";
// An auxiliary component is commonly used for wrapping
// adjacente elements, satisfying React's requirement
// of elements being wrapped in an enclosing tag.
const Auxiliary = (props) => props.children;

export default Auxiliary;
